---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: helmreleases.app.siji.io
spec:
  group: app.siji.io
  names:
    kind: HelmRelease
    listKind: HelmReleaseList
    plural: helmreleases
    singular: helmrelease
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.namespace
      name: NAMESPACE
      type: string
    - jsonPath: .status.revision
      name: REVISION
      type: string
    - jsonPath: .status.status
      name: STATUS
      type: string
    - jsonPath: .status.chart
      name: CHART
      type: string
    - jsonPath: .status.updated
      name: UPDATED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: HelmRelease is the Schema for the helmreleases API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HelmReleaseSpec defines the desired state of HelmRelease
            properties:
              chart:
                description: Chart is Helm Chart object
                properties:
                  address:
                    description: Helm Chart address
                    type: string
                  credential:
                    description: Credential is the secret that contains user/pass
                      to download the chart
                    type: string
                  skipTLSVerify:
                    description: skipTLSVerify skips the TLS verify when downloading
                      the chart
                    type: boolean
                required:
                - address
                type: object
              values:
                description: Values is Helm Release values
                type: object
                x-kubernetes-preserve-unknown-fields: true
            required:
            - chart
            type: object
          status:
            description: HelmReleaseStatus defines the observed state of HelmRelease
            properties:
              chart:
                description: Chart is chart that used for the Helm Release
                type: string
              hashedSpec:
                description: HashedSpec is the hashcode of HelmReleaseSpec
                type: string
              revision:
                description: Revision is the Helm Release revision
                type: string
              status:
                description: Status is the Helm Release status
                type: string
              updated:
                description: Updated is the Helm Release last updated time
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
